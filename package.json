{
  "name": "ChordLine",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "bootstrap": "pnpm install",
    "dev": "concurrently \"pnpm --filter web run dev\" \"pnpm --filter api run start:dev\" -n web,api",
    "prebuild": "pnpm --filter @chordline/types run build",
    "build": "pnpm -w -r run build",
    "test": "pnpm -w -r run test",
    "lint": "pnpm -w -r run lint"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.12.4",
  "devDependencies": {
    "supabase": "^2.51.0",
    "concurrently": "^7.6.0"
  }
}
